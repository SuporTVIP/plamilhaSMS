name: PramilhaSVIP
description: "A new Flutter project."
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
    
  
  http: ^1.2.0
  dio: ^5.4.0
  uuid: ^4.3.3
  shared_preferences: ^2.2.0
  firebase_messaging: ^16.1.1
  firebase_core: ^4.4.0
  
  webview_flutter: ^4.7.0
  audioplayers: ^6.0.0
  package_info_plus: ^8.0.0
  # ❌ workmanager removido — substituído pela arquitetura 100% Push via FCM
  

  google_fonts: ^8.0.2
  flutter_animate: ^4.5.0
  url_launcher: ^6.3.0
  flutter_local_notifications: ^20.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  permission_handler: ^12.0.1
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/logo.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
     - assets/sounds/
     - assets/sounds/intro.mp3
     - assets/images/